@AGENTS.md

# Local Development Rules

- Always work directly inside the current project: C:\Users\ERCE\Desktop\Vela
- Never create or return a ZIP, archive, duplicate project, temporary project copy, or full-project export unless I explicitly ask for one.
- Before editing, inspect the existing local files.
- Edit only the minimum necessary files.
- Never modify package.json, package-lock.json, Expo SDK version, app.json, or install/remove dependencies unless I explicitly ask.
- Preserve existing working functionality and architecture.
- After every task, verify that the modified files actually exist under C:\Users\ERCE\Desktop\Vela.
- At the end, report only the files actually changed and a concise summary.
- Never claim a file was modified unless you modified the local file in this working directory.
